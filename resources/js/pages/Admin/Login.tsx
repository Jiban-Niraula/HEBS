import { Head, navigate, useForm } from '@/runtime';

export default function Login() {
  const form = useForm({ email: '', password: '', remember: false });
  return <main className="login-shell"><Head title="Admin login" /><section className="login-panel"><div className="admin-brand"><span className="admin-brand-mark"><img src="/images/hebs-facebook-profile.jpg" alt="HEBS logo" /></span><div><strong>HEBS</strong><small>School operations</small></div></div><p className="admin-kicker"><i className="ph-fill ph-lock-key mr-1" />Secure access</p><h1>Sign in to the school office.</h1><p>Manage official content, admissions, notices, and public information.</p><form className="admin-form" onSubmit={(event) => { event.preventDefault(); form.post('/admin/login', { onSuccess: () => navigate('/admin') }); }}><label>Email address<input type="email" autoComplete="email" value={form.data.email} onChange={(event) => form.setData('email', event.target.value)} required />{form.errors.email ? <small className="form-error">{form.errors.email}</small> : null}</label><label>Password<input type="password" autoComplete="current-password" value={form.data.password} onChange={(event) => form.setData('password', event.target.value)} required />{form.errors.password ? <small className="form-error">{form.errors.password}</small> : null}</label><label className="consent"><input type="checkbox" checked={form.data.remember} onChange={(event) => form.setData('remember', event.target.checked)} /> Remember this device</label><button className="button primary" disabled={form.processing}><i className="ph-bold ph-sign-in mr-2" />{form.processing ? 'Signing in...' : 'Sign in'}</button></form><a className="text-link" href="/"><i className="ph-bold ph-arrow-left mr-1" />Return to public website</a></section></main>;
}
